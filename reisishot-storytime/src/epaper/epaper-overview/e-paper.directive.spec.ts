import {EPaperDirective} from './e-paper.directive';

describe('EPaperDirective', () => {
  it('should create an instance', () => {
    const directive = new EPaperDirective();
    expect(directive).toBeTruthy();
  });
});
